A JavaFX desktop application for tracking electronic health records. Patient database included.

To run the project, clone the repository, install Maven dependencies, and then run Launcher.java.

SQLite database included with all the tables needed, as well as mock data.
